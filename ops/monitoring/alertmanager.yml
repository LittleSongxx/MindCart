# Alertmanager 最小可用配置：分组路由 + 重复抑制。
# default receiver 无集成的接收端（通知落空但不报错）——
# 生产接入时在 receivers 里补 webhook/邮件等真实通道即可。
global:
  resolve_timeout: 5m
route:
  group_by: ['alertname']
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h
  receiver: default
receivers:
  - name: default
