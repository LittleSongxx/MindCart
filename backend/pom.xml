<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>3.5.16</version>
    <relativePath/>
  </parent>

  <groupId>com.smartore</groupId>
  <artifactId>smartore-backend</artifactId>
  <version>0.1.0-SNAPSHOT</version>
  <packaging>pom</packaging>
  <name>smartore-backend</name>
  <description>Smartore AI shopping mall, Spring Cloud microservices</description>

  <modules>
    <module>smartore-common</module>
    <module>smartore-gateway</module>
    <module>smartore-user</module>
    <module>smartore-goods</module>
    <module>smartore-trade</module>
    <module>smartore-ai</module>
    <!-- 语音导购服务：新增互补，独立于 smartore-ai（ADR-0011） -->
    <module>smartore-voice</module>
  </modules>

  <properties>
    <java.version>21</java.version>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <spring-cloud.version>2025.0.3</spring-cloud.version>
    <spring-cloud-alibaba.version>2025.0.0.0</spring-cloud-alibaba.version>
    <spring-ai.version>1.1.7</spring-ai.version>
    <mybatis-spring-boot.version>3.0.5</mybatis-spring-boot.version>
    <pagehelper.version>2.1.0</pagehelper.version>
    <hutool.version>5.8.25</hutool.version>
    <java-jwt.version>4.4.0</java-jwt.version>
    <!-- JaCoCo 不在 Boot BOM 里，需显式钉版本 -->
    <jacoco.version>0.8.13</jacoco.version>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.springframework.cloud</groupId>
        <artifactId>spring-cloud-dependencies</artifactId>
        <version>${spring-cloud.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>com.alibaba.cloud</groupId>
        <artifactId>spring-cloud-alibaba-dependencies</artifactId>
        <version>${spring-cloud-alibaba.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <!-- Spring AI BOM：只有 ai 服务使用（OpenAI 兼容协议接 DeepSeek/百炼） -->
      <dependency>
        <groupId>org.springframework.ai</groupId>
        <artifactId>spring-ai-bom</artifactId>
        <version>${spring-ai.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>

      <!-- 内部契约模块：消费方只依赖 api，不依赖 app 实现（契约先行） -->
      <dependency>
        <groupId>com.smartore</groupId>
        <artifactId>smartore-common</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>com.smartore</groupId>
        <artifactId>smartore-user-api</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>com.smartore</groupId>
        <artifactId>smartore-goods-api</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>com.smartore</groupId>
        <artifactId>smartore-trade-api</artifactId>
        <version>${project.version}</version>
      </dependency>

      <dependency>
        <groupId>org.mybatis.spring.boot</groupId>
        <artifactId>mybatis-spring-boot-starter</artifactId>
        <version>${mybatis-spring-boot.version}</version>
      </dependency>
      <dependency>
        <groupId>com.github.pagehelper</groupId>
        <artifactId>pagehelper-spring-boot-starter</artifactId>
        <version>${pagehelper.version}</version>
      </dependency>
      <dependency>
        <groupId>cn.hutool</groupId>
        <artifactId>hutool-all</artifactId>
        <version>${hutool.version}</version>
      </dependency>
      <dependency>
        <groupId>com.auth0</groupId>
        <artifactId>java-jwt</artifactId>
        <version>${java-jwt.version}</version>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <!-- 所有模块共有的依赖：Testcontainers 与 Lombok 由 Boot BOM 管版本 -->
  <dependencies>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>mysql</artifactId>
      <scope>test</scope>
    </dependency>
    <!-- 在父 pom 统一声明：此前只有 voice 用 Lombok，其余模块手写实体存取器，同一份代码库两种风格。
         optional 保证不向 api 契约模块的使用方传递。 -->
    <dependency>
      <groupId>org.projectlombok</groupId>
      <artifactId>lombok</artifactId>
      <optional>true</optional>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <!-- 覆盖率：report 绑在 test 阶段而不是 verify —— dev.sh test 与 CI 都只跑 mvn test，
           绑到 verify 会让"跑了测试却没有报告"。只出报告不设阈值门禁：
           阈值在演示/迭代期容易卡住构建，收益不如让数字可见。 -->
      <plugin>
        <groupId>org.jacoco</groupId>
        <artifactId>jacoco-maven-plugin</artifactId>
        <version>${jacoco.version}</version>
        <executions>
          <execution>
            <id>prepare-agent</id>
            <goals>
              <goal>prepare-agent</goal>
            </goals>
          </execution>
          <execution>
            <id>report</id>
            <phase>test</phase>
            <goals>
              <goal>report</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.springframework.boot</groupId>
          <artifactId>spring-boot-maven-plugin</artifactId>
          <!-- Lombok 只在编译期生效，打进 fat jar 只是白占体积 -->
          <configuration>
            <excludes>
              <exclude>
                <groupId>org.projectlombok</groupId>
                <artifactId>lombok</artifactId>
              </exclude>
            </excludes>
          </configuration>
        </plugin>
      </plugins>
    </pluginManagement>
  </build>
</project>
