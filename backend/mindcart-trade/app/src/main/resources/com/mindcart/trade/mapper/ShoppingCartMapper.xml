<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mindcart.trade.mapper.ShoppingCartMapper">

    <!-- 商品展示信息由服务层经 goods 服务实时回填（selectMine），购物车表只存自有列；
         旧版 selectAll 曾 left join product（跨库），已随死代码一并移除 -->
    <delete id="deleteById">
        delete from shopping_cart
        where id = #{id}
    </delete>

    <insert id="insert" parameterType="com.mindcart.trade.entity.ShoppingCart" useGeneratedKeys="true" keyProperty="id">
        insert into shopping_cart
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null">id,</if>
            <if test="userId != null">user_id,</if>
            <if test="productId != null">product_id,</if>
            <if test="quantity != null">quantity,</if>
            <if test="selected != null">selected,</if>
            <if test="createTime != null">create_time,</if>
            <if test="updateTime != null">update_time,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="id != null">#{id},</if>
            <if test="userId != null">#{userId},</if>
            <if test="productId != null">#{productId},</if>
            <if test="quantity != null">#{quantity},</if>
            <if test="selected != null">#{selected},</if>
            <if test="createTime != null">#{createTime},</if>
            <if test="updateTime != null">#{updateTime},</if>
        </trim>
    </insert>

    <update id="updateById" parameterType="com.mindcart.trade.entity.ShoppingCart">
        update shopping_cart
        <set>
            <if test="quantity != null">quantity = #{quantity},</if>
            <if test="selected != null">selected = #{selected},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </set>
        where id = #{id}
    </update>

</mapper>
